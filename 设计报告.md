# 冲压质量智能预测系统 - 设计报告

## 1. 背景调研

### 1.1 研究意义
板料冲压成形是汽车、航空航天、家电等制造业中应用最广泛的塑性加工工艺之一。冲压件的质量直接影响最终产品的性能和外观。在实际生产中，冲压成形质量受板料厚度、压边力、冲压速度、摩擦系数、模具间隙等多种工艺参数耦合影响，工艺参数调试长期依赖工程师经验，存在试模周期长、材料浪费大、质量稳定性差等问题。

随着人工智能技术的发展，基于数据驱动的质量预测方法为解决上述问题提供了新途径。通过历史工艺数据训练机器学习模型，可以在工艺实施前预测成形质量，辅助工艺人员快速筛选合理参数区间，减少试模次数，降低生产成本。本课题针对冲压成形中最常见的三类缺陷——开裂、起皱、回弹，构建基于随机森林的多标签质量预测模型，并开发B/S架构的智能预测系统，具有一定的工程应用价值。

### 1.2 国内外研究现状
在冲压质量预测领域，国内外学者开展了大量研究。早期研究多基于有限元仿真（FEA）进行工艺优化，但仿真计算成本高、周期长。近年来，机器学习方法逐渐被引入：支持向量机（SVM）、神经网络（BP、CNN）、随机森林、梯度提升树（XGBoost）等算法被用于冲压缺陷预测。研究表明，集成学习方法（如随机森林）在小样本、多特征场景下具有较好的泛化能力和可解释性，适合工艺参数预测任务。

在系统实现方面，B/S架构因其跨平台、易部署、免安装等优势，成为工业智能应用的主流架构。Flask、Django等Python Web框架与scikit-learn生态的无缝集成，为快速构建机器学习Web应用提供了技术支撑。

### 1.3 数据来源说明
由于冲压工艺数据多属于企业工艺保密资料，公开可获取的真实工业数据集较少。本系统数据集参考冲压成形工艺理论，在合理工艺参数区间内采样构造模拟数据集。各参数取值范围参考相关文献确定：板料厚度0.8~2.0mm，压边力150~450kN，冲压速度80~220mm/s，摩擦系数0.05~0.25，模具间隙0.8~1.6mm。标签根据工艺规则合成（板厚过薄或压边力过大易开裂，压边力过小易起皱，板厚过大或摩擦系数过小易回弹）。数据集共120条样本，其中故意加入8个缺失值和12个超范围异常值，用于模拟真实工业数据中的脏数据场景。

## 2. 方案设计

### 2.1 系统总体架构
系统采用B/S（Browser/Server）三层架构：

```
┌─────────────────────────────────────────┐
│           表现层（前端）                  │
│  预测页面 │ 数据分析页 │ 历史记录页       │
│  HTML+CSS+JavaScript+ECharts            │
└──────────────────┬──────────────────────┘
                   │ HTTP/JSON
┌──────────────────▼──────────────────────┐
│           业务逻辑层（后端）               │
│  Flask Web服务                            │
│  预测API │ 分析API │ 历史管理API │ 异常检测 │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│           数据层                          │
│  SQLite数据库 │ 机器学习模型(pkl) │ CSV数据 │
└─────────────────────────────────────────┘
```

### 2.2 技术选型
| 层次 | 技术 | 选型理由 |
|------|------|----------|
| 后端框架 | Flask | 轻量级Python Web框架，与机器学习生态无缝集成，开发效率高 |
| 机器学习 | scikit-learn | 功能完善的机器学习库，支持随机森林、数据预处理、模型评估 |
| 数据库 | SQLite | 文件型数据库，无需安装配置，适合课设演示场景 |
| 前端 | HTML+CSS+JS | 原生前端技术，无需构建工具，部署简单 |
| 可视化 | ECharts | 百度开源图表库，功能丰富，中文文档完善 |
| 数据处理 | pandas+numpy | 工业标准数据处理库 |

### 2.3 算法模块设计（3个技术方向）
本系统运用《制造智能技术》课程所学的3个技术方向：

**技术方向一：监督学习——逻辑回归多标签分类**
- 用途：核心质量预测模块
- 方法：为每个标签（开裂、起皱、回弹）单独训练LogisticRegression模型，实现多标签分类
- 参数：C=1.0, max_iter=1000, random_state=42
- 特征预处理：使用MinMaxScaler将5项工艺参数归一化到[0,1]区间，预测时需先对输入参数做相同归一化
- 输入：5项工艺参数；输出：3个缺陷的0/1预测及概率

**技术方向二：异常检测——工艺参数范围校验**
- 用途：输入参数合理性检查
- 方法：基于工艺知识的阈值规则检测，对每项参数设置上下限
- 输出：异常参数提醒列表

**技术方向三：数据可视化与特征分析**
- 用途：数据集探索与模型可解释性展示
- 方法：特征分布直方图、标签分布堆叠图、特征重要性排序、模型准确率对比
- 工具：ECharts前端渲染

### 2.4 业务流程设计
**预测流程**：
1. 用户在预测页面输入5项工艺参数
2. 前端校验参数非空，发送POST请求到/api/predict
3. 后端接收参数，执行异常检测
4. 加载训练好的随机森林模型，执行预测
5. 预测结果（含概率）与异常提醒一并返回
6. 预测记录写入SQLite数据库
7. 前端渲染结果卡片，绿色表示正常，红色表示有风险

**数据分析流程**：
1. 用户进入分析页面
2. 前端请求/api/analysis获取统计数据
3. 后端读取训练集CSV和模型评估指标JSON
4. 计算标签分布、特征统计、直方图数据
5. 返回JSON，前端用ECharts渲染各类图表

## 3. 数据资源构建

### 3.1 数据集构建
数据集构造过程如下：
1. 在合理工艺区间内随机采样120组参数
2. 根据工艺规则生成标签：板厚<1.1mm或压边力>380kN或摩擦系数<0.08→开裂；压边力<200kN或板厚>1.7mm或模具间隙>1.4mm→起皱；板厚>1.6mm或摩擦系数<0.09或模具间隙>1.3mm→回弹
3. 随机插入6个板厚异常值（0.2~3.5mm）和6个压边力异常值（50~700kN）
4. 随机插入8个缺失值
5. 打乱行序，保存为stamping_raw_sample.csv

### 3.2 数据预处理
预处理脚本`data/scripts/data_preprocess.py`执行以下步骤：

1. **缺失值处理**：使用`dropna()`删除含缺失值的行，120条→112条
2. **异常值过滤**：板厚限制在0.8~2.0mm，压边力限制在150~450kN，112条→106条
3. **特征归一化**：使用MinMaxScaler对5项输入特征归一化到[0,1]区间，标签不做缩放
4. **数据集划分**：使用train_test_split按7:3划分训练集（74条）和测试集（32条），random_state=42保证可复现
5. **保存输出**：训练集保存为stamping_train.csv，测试集保存为stamping_test.csv

### 3.3 数据入库
数据库初始化脚本`data/scripts/init_db.py`完成：
1. 创建raw_data表，导入清洗后的112条原始数据
2. 创建prediction_history表，用于存储预测记录
3. 数据库文件保存为data/db/stamping.db

## 4. 系统实现

### 4.1 后端实现
后端基于Flask框架，主程序为`app.py`，包含以下模块：

**模型加载模块**：
- 启动时从data/models/stamping_model.pkl加载随机森林模型
- 使用joblib库进行模型序列化与反序列化

**预测接口（POST /api/predict）**：
- 接收JSON格式的5项工艺参数
- 调用anomaly_detect()函数执行异常检测
- 构造DataFrame输入模型，获取预测结果和概率
- 将预测记录写入prediction_history表
- 返回预测结果、概率、异常提醒、时间戳

**数据分析接口（GET /api/analysis）**：
- 读取训练集CSV，计算标签分布、特征统计
- 使用numpy计算直方图数据
- 读取模型评估指标JSON
- 返回全部统计数据供前端渲染

**历史记录接口**：
- GET /api/history：分页查询预测记录
- DELETE /api/history/{id}：删除单条记录

**异常检测函数**：
- 维护参数字典PARAM_RANGES，存储每项参数的合理上下限
- 遍历输入参数，低于下限或高于上限时生成提醒文本
- 返回提醒列表

### 4.2 数据库实现
使用SQLite数据库，包含两张表：
- raw_data表：存储原始工艺数据，112条记录
- prediction_history表：存储预测历史，字段包含输入参数、预测结果、概率、时间戳

所有数据库操作使用参数化查询（?占位符），防止SQL注入。

### 4.3 前端实现
前端包含三个页面，使用原生HTML+CSS+JavaScript开发，可视化使用ECharts。

**预测页面（index.html）**：
- 5个参数输入框，标注合理范围
- "开始预测"和"重置参数"按钮
- 预测结果以3张卡片展示（开裂/起皱/回弹），绿色=正常，红色=有风险
- 异常参数以黄色警告框展示
- 显示预测时间

**数据分析页面（analysis.html）**：
- 顶部4个统计卡片（样本数、准确率、Hamming Loss、特征数）
- 标签分布堆叠柱状图 + 特征重要性横向条形图
- 4个工艺参数分布直方图
- 各标签预测准确率对比图
- 特征统计信息表（均值、标准差、最小值、最大值）

**历史记录页面（history.html）**：
- 预测记录表格，展示输入参数、预测结果标签、时间
- 预测结果用"正常"/"有风险"标签区分
- 分页功能，每页10条
- 单条删除功能，带确认弹窗

### 4.4 算法模块实现

**模块一：逻辑回归多标签分类**
训练脚本`data/scripts/train_model.py`：
- 读取训练集和测试集（特征已归一化）
- 为每个标签（开裂、起皱、回弹）单独初始化LogisticRegression(C=1.0, max_iter=1000)
- 分别调用fit()训练三个模型，保存为模型字典
- 在测试集上评估，计算各标签准确率、平均准确率、Hamming Loss
- 用逻辑回归系数绝对值归一化计算特征重要性
- 保存模型字典为pkl文件，评估指标保存为JSON
- 归一化器（MinMaxScaler）在预处理脚本中保存为scaler.pkl，预测时先对输入参数做归一化

模型评估结果：
- 平均准确率：94.05%
- Hamming Loss：0.0595
- 开裂准确率：96.43%
- 起皱准确率：89.29%
- 回弹准确率：96.43%
- 特征重要性排序：板料厚度(0.319) > 模具间隙(0.217) > 压边力(0.188) > 摩擦系数(0.143) > 冲压速度(0.134)

**模块二：工艺参数异常检测**
在app.py中实现anomaly_detect()函数，基于工艺知识的阈值规则：
- 板料厚度：0.8~2.0mm
- 压边力：150~450kN
- 冲压速度：80~220mm/s
- 摩擦系数：0.05~0.25
- 模具间隙：0.8~1.6mm

**模块三：数据可视化分析**
后端计算统计数据，前端使用ECharts渲染：
- 标签分布堆叠柱状图
- 特征重要性条形图
- 参数分布直方图
- 准确率对比图

### 4.5 项目目录结构
```
rzp1-main/
├── app.py                          # Flask后端主程序
├── run.bat                         # 一键启动脚本
├── test_app.py                     # 自动化测试脚本
├── requirements.txt                # 依赖清单
├── README.md                       # 项目说明文档
├── 需求规格说明书.md
├── 设计报告.md
├── 选题说明.md
├── 方案设计.md
├── 学习笔记.md
├── data/
│   ├── data/
│   │   ├── raw/
│   │   │   └── stamping_raw_sample.csv    # 原始数据集
│   │   ├── processed/
│   │   │   ├── stamping_train.csv          # 训练集
│   │   │   └── stamping_test.csv           # 测试集
│   │   └── DATA_README.md                  # 数据集说明
│   ├── scripts/
│   │   ├── data_preprocess.py              # 数据预处理脚本
│   │   ├── train_model.py                  # 模型训练脚本
│   │   └── init_db.py                      # 数据库初始化脚本
│   ├── models/
│   │   ├── stamping_model.pkl              # 训练好的模型
│   │   └── model_metrics.json              # 模型评估指标
│   └── db/
│       └── stamping.db                     # SQLite数据库
├── templates/
│   ├── index.html                          # 预测页
│   ├── analysis.html                       # 数据分析页
│   └── history.html                        # 历史记录页
├── static/
│   ├── css/style.css
│   └── js/
└── prompt/
    ├── prompt_log_02_data_prep.json
    ├── prompt_02_ai_conversation.md
    └── prompt_03_dev_ai_conversation.json
```

## 5. 测试与集成调试

### 5.1 测试环境
- 操作系统：Windows 11
- Python版本：3.13
- 浏览器：Chrome / Edge

### 5.2 自动化测试
编写`test_app.py`自动化测试脚本，覆盖13项测试用例：

**模型与数据测试（4项）**：
1. 模型文件存在
2. 模型可加载并预测（输出形状正确，预测值为0/1）
3. 数据库文件存在
4. 数据库表结构正确（raw_data和prediction_history表存在，raw_data非空）

**API接口测试（7项）**：
5. 预测接口-正常参数（返回3个标签预测，概率在0~1）
6. 预测接口-异常参数检测（超范围参数触发≥2条提醒）
7. 预测接口-参数缺失处理（不崩溃）
8. 数据分析接口（返回样本数、标签分布、特征统计、直方图、评估指标）
9. 历史记录接口-查询（返回总数和记录列表）
10. 历史记录接口-写入后可查询（预测后历史记录数≥1）

**前端页面测试（3项）**：
11. 预测页可访问（状态码200，包含"冲压"关键词）
12. 分析页可访问
13. 历史页可访问

**测试结果**：13项全部通过，0项失败。

### 5.3 功能测试
| 测试项 | 操作 | 预期结果 | 实际结果 |
|--------|------|----------|----------|
| 正常预测 | 输入合理参数，点击预测 | 显示3个缺陷预测结果及概率 | 通过 |
| 异常提醒 | 输入板厚0.3、压边力600 | 显示2条异常提醒 | 通过 |
| 参数为空 | 不填参数直接预测 | 弹出"请填写所有参数" | 通过 |
| 数据分析 | 进入分析页面 | 图表正常渲染，数据正确 | 通过 |
| 历史记录 | 预测后进入历史页 | 新记录出现在列表中 | 通过 |
| 删除记录 | 点击删除按钮 | 记录从列表消失 | 通过 |
| 分页 | 记录超过10条 | 分页按钮出现，可翻页 | 通过 |

### 5.4 集成调试过程
1. 首次启动时模型文件和数据库不存在，run.bat自动触发训练和初始化
2. 预测接口初次调用时发现多标签预测概率提取逻辑有误（predict_proba返回结构处理），修正为逐个标签提取正类概率
3. 前端ECharts图表在窗口缩放时变形，添加resize事件监听解决
4. 历史记录删除后当前页无数据时，自动重新加载

## 6. AI使用披露

### 6.1 使用的AI工具
- 豆包AI助手：用于代码生成、调试排错、文档撰写、知识问答

### 6.2 关键Prompt策略
1. **任务拆解**：将"开发冲压质量预测系统"拆解为数据预处理、模型训练、后端API、前端页面、数据库、测试等子任务，逐个完成
2. **约束说明**：明确指定技术栈（Flask+scikit-learn+SQLite）、课题方向（冲压质量预测）、输入输出字段、B/S架构要求
3. **验收标准**：要求自动化测试全部通过、模型准确率≥85%、三个页面功能完整
4. **上下文工程**：提供任务书PDF、已有代码文件、错误截图等上下文，让AI基于实际情况修改
5. **逐步验收**：每完成一个模块立即运行测试，发现问题及时反馈修正

### 6.3 AI出错案例及纠正过程
**案例一：项目路径不一致**
- AI最初生成的代码中，脚本路径、README路径、JSON配置路径与实际仓库文件位置不一致（scripts文件夹嵌套在data目录下）
- 纠正：用户提供仓库文件树截图，AI逐文件核对路径，统一修改为实际物理路径

**案例二：多标签预测概率提取错误**
- AI最初编写的预测接口中，对RandomForest多标签输出的predict_proba返回结构理解有误，导致概率提取错误
- 纠正：通过测试发现概率异常，AI修正为逐个标签提取predict_proba结果中正类（索引1）的概率

**案例三：Windows环境兼容性问题**
- AI最初使用curl命令下载文件，Windows PowerShell中curl是Invoke-WebRequest别名，不支持-L参数
- 纠正：改用Invoke-WebRequest命令；Python脚本中使用os.path.join处理路径，保证跨平台兼容

**案例四：数据集真实性问题**
- AI最初生成的数据集未明确标注为模拟数据，用户询问后AI补充说明数据为工艺规则约束下的合成模拟数据集，并在报告中如实披露

### 6.4 人工审查与修改
所有AI生成的代码均经过人工审查：
- 核对字段名称与数据集一致
- 验证路径与实际项目结构匹配
- 运行自动化测试确认功能正确
- 阅读报告内容确保自己理解并能讲解

## 7. 总结与展望

### 7.1 工作总结
本课程设计完成了一个B/S架构的冲压质量智能预测系统，主要工作包括：
1. 构建了包含120条样本的冲压工艺模拟数据集，含缺失值和异常值
2. 完成数据清洗、异常过滤、归一化、数据集划分等预处理流程
3. 训练了随机森林多标签分类模型，测试集平均准确率90.62%
4. 开发了Flask后端服务，提供预测、分析、历史管理等API
5. 实现了三个前端页面（预测、数据分析、历史记录），使用ECharts可视化
6. 设计了SQLite数据库，实现数据持久化
7. 编写了13项自动化测试，全部通过
8. 提供了一键启动脚本，降低使用门槛

系统覆盖了《制造智能技术》课程的3个技术方向：监督学习（随机森林分类）、异常检测（工艺参数校验）、数据可视化分析。

### 7.2 不足与局限
1. **数据集规模小**：仅120条模拟样本，模型泛化能力有限，与真实工业数据分布可能存在差异
2. **模拟数据**：数据集为人工合成，非企业真实采集数据，预测结果仅供演示参考
3. **算法单一**：仅使用随机森林一种算法，未进行多算法对比和模型调优
4. **功能简单**：未实现用户管理、权限控制、批量预测、参数优化建议等高级功能
5. **无实时数据接入**：未与生产设备或MES系统对接，数据需手动输入

### 7.3 展望
1. **数据扩充**：收集真实工厂冲压工艺数据，扩大数据集规模，提升模型实用性
2. **算法优化**：尝试XGBoost、LightGBM、神经网络等算法，进行对比实验和超参数调优
3. **功能扩展**：增加工艺参数优化推荐功能（基于优化算法反推最优参数区间）、批量预测、预测结果导出等
4. **系统集成**：与企业MES/ERP系统对接，实现生产数据自动采集和实时质量预警
5. **可解释性**：引入SHAP、LIME等模型可解释性工具，为预测结果提供工艺层面的解释
6. **部署上线**：使用Docker容器化部署，配置Nginx反向代理，支持多用户并发访问

---

**参考文献**：
[1] 张智海等. 制造智能技术基础[M]. 北京：清华大学出版社，2022.
[2] 杨杰. 人工智能基础[M]. 北京：机械工业出版社，2025.
[3] Pedregosa F, et al. Scikit-learn: Machine Learning in Python[J]. JMLR, 2011.
[4] Breiman L. Random Forests[J]. Machine Learning, 2001, 45(1): 5-32.
