# 冲压质量智能预测系统 - 需求规格说明书

## 1. 引言

### 1.1 编写目的
本文档为"冲压质量智能预测系统"的需求规格说明书，旨在明确系统的功能需求、非功能需求、数据需求和接口需求，为系统设计、开发、测试和验收提供依据。

### 1.2 项目背景
板料冲压成形是制造业中广泛应用的加工工艺，其成形质量受板料厚度、压边力、冲压速度、摩擦系数、模具间隙等多种工艺参数影响。传统工艺调试依赖经验，存在周期长、成本高的问题。本系统运用机器学习技术，基于历史工艺数据构建质量预测模型，实现冲压成形缺陷（开裂、起皱、回弹）的智能预测，辅助工艺人员快速优化参数。

### 1.3 术语定义
| 术语 | 说明 |
|------|------|
| 冲压成形 | 利用模具对板料施加压力，使其产生塑性变形获得所需形状零件的工艺 |
| 开裂 | 板料在冲压过程中因拉应力超过材料极限而产生的裂纹缺陷 |
| 起皱 | 板料在压应力作用下发生失稳产生的波浪形褶皱缺陷 |
| 回弹 | 冲压件脱模后因弹性恢复导致的形状尺寸偏差 |
| 压边力 | 冲压时压边圈对板料施加的夹持力，用于防止起皱 |
| B/S架构 | Browser/Server，浏览器/服务器架构 |

## 2. 系统概述

### 2.1 系统目标
构建一个B/S架构的冲压质量智能预测系统，实现以下目标：
1. 基于工艺参数预测冲压件是否出现开裂、起皱、回弹缺陷
2. 对输入工艺参数进行异常检测，提醒参数是否超出合理范围
3. 提供数据集分析与模型评估可视化展示
4. 记录预测历史，支持查询与管理

### 2.2 用户角色
| 角色 | 说明 | 权限 |
|------|------|------|
| 工艺工程师 | 系统主要使用者 | 使用预测功能、查看分析、管理历史记录 |
| 系统管理员 | 维护系统运行 | 全部功能权限 |

### 2.3 运行环境
- **服务端**：Python 3.8+、Flask、scikit-learn、pandas、SQLite
- **客户端**：现代浏览器（Chrome、Edge、Firefox），支持JavaScript
- **操作系统**：Windows 10/11、Linux

## 3. 功能需求

### 3.1 质量预测功能（FR-001）
**功能描述**：用户输入5项工艺参数，系统调用机器学习模型预测3类质量缺陷。

**输入参数**：
| 参数名 | 类型 | 单位 | 合理范围 | 说明 |
|--------|------|------|----------|------|
| sheet_thickness | float | mm | 0.8~2.0 | 板料厚度 |
| blank_holder_force | float | kN | 150~450 | 压边力 |
| stamping_speed | float | mm/s | 80~220 | 冲压速度 |
| friction_coeff | float | - | 0.05~0.25 | 摩擦系数 |
| die_gap | float | mm | 0.8~1.6 | 模具间隙 |

**输出结果**：
| 输出项 | 类型 | 说明 |
|--------|------|------|
| 开裂预测 | 0/1 + 概率 | 是否存在开裂风险及风险概率 |
| 起皱预测 | 0/1 + 概率 | 是否存在起皱风险及风险概率 |
| 回弹预测 | 0/1 + 概率 | 是否存在回弹风险及风险概率 |
| 异常提醒 | 文本列表 | 参数超出合理范围的提醒信息 |

**业务规则**：
1. 所有参数必填，为空时提示用户填写
2. 预测结果自动保存到历史记录
3. 模型使用随机森林多标签分类算法

### 3.2 工艺参数异常检测功能（FR-002）
**功能描述**：在预测的同时，检测输入参数是否在工艺合理范围内，对异常参数给出提醒。

**业务规则**：
1. 参数低于下限或高于上限时，生成对应提醒
2. 多个参数异常时，全部列出
3. 异常检测不阻断预测流程，仅作为辅助提醒

### 3.3 数据分析功能（FR-003）
**功能描述**：展示训练数据集的统计分析和模型评估结果。

**展示内容**：
1. 训练样本总数、模型平均准确率、Hamming Loss、特征数等核心指标
2. 质量缺陷标签分布（开裂/起皱/回弹的正负样本数）
3. 特征重要性排序
4. 各工艺参数分布直方图
5. 各标签预测准确率对比
6. 特征统计信息表（均值、标准差、最小值、最大值）

### 3.4 预测历史管理功能（FR-004）
**功能描述**：记录所有预测操作，支持分页查询和删除。

**功能点**：
1. 列表展示历史预测记录（ID、工艺参数、预测结果、预测时间）
2. 分页浏览，每页10条
3. 单条记录删除
4. 预测结果用标签区分"正常"和"有风险"

### 3.5 系统初始化功能（FR-005）
**功能描述**：首次运行时自动完成模型训练和数据库初始化。

**功能点**：
1. 模型文件不存在时，自动运行训练脚本生成模型
2. 数据库不存在时，自动创建表结构并导入原始数据
3. 一键启动脚本完成依赖检查、模型检查、服务启动

## 4. 非功能需求

### 4.1 性能需求
- 单次预测响应时间 ≤ 2秒
- 页面加载时间 ≤ 3秒
- 支持并发用户数 ≥ 10人

### 4.2 可用性需求
- 界面简洁直观，工艺工程师无需培训即可使用
- 输入参数提供合理范围提示
- 预测结果用颜色区分风险等级（绿色=正常，红色=有风险）
- 操作异常时给出明确错误提示

### 4.3 可靠性需求
- 模型预测准确率 ≥ 85%
- 系统崩溃后可通过一键启动脚本恢复
- 数据持久化存储，重启不丢失

### 4.4 可维护性需求
- 代码结构清晰，模块化设计
- 提供自动化测试脚本，覆盖核心功能
- 配置与代码分离，路径统一管理

### 4.5 安全性需求
- 用户输入进行类型校验，防止非法输入
- 数据库操作使用参数化查询，防止SQL注入
- 仅监听本地回环地址（127.0.0.1），不对外暴露

## 5. 数据需求

### 5.1 数据集说明
- **数据来源**：参考冲压成形工艺理论，在合理工艺区间内采样构造的模拟数据集
- **样本量**：原始120条（含缺失值和异常样本），清洗后106条
- **划分比例**：训练集70%（74条），测试集30%（32条）

### 5.2 数据库设计
**表1：raw_data（原始数据表）**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| sheet_thickness | REAL | 板料厚度 |
| blank_holder_force | REAL | 压边力 |
| stamping_speed | REAL | 冲压速度 |
| friction_coeff | REAL | 摩擦系数 |
| die_gap | REAL | 模具间隙 |
| crack | INTEGER | 开裂标签（0/1） |
| wrinkle | INTEGER | 起皱标签（0/1） |
| springback | INTEGER | 回弹标签（0/1） |

**表2：prediction_history（预测历史表）**
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键，自增 |
| sheet_thickness | REAL | 板料厚度 |
| blank_holder_force | REAL | 压边力 |
| stamping_speed | REAL | 冲压速度 |
| friction_coeff | REAL | 摩擦系数 |
| die_gap | REAL | 模具间隙 |
| pred_crack | INTEGER | 预测开裂结果 |
| pred_wrinkle | INTEGER | 预测起皱结果 |
| pred_springback | INTEGER | 预测回弹结果 |
| crack_prob | REAL | 开裂概率 |
| wrinkle_prob | REAL | 起皱概率 |
| springback_prob | REAL | 回弹概率 |
| created_at | TEXT | 预测时间 |

## 6. 接口需求

### 6.1 预测接口
- **路径**：POST /api/predict
- **请求体**：JSON，包含5项工艺参数
- **响应**：JSON，包含预测结果、概率、异常提醒、时间戳

### 6.2 数据分析接口
- **路径**：GET /api/analysis
- **响应**：JSON，包含样本统计、标签分布、特征统计、直方图数据、模型评估指标

### 6.3 历史记录查询接口
- **路径**：GET /api/history?page=1&size=10
- **响应**：JSON，包含总记录数、当前页记录列表

### 6.4 历史记录删除接口
- **路径**：DELETE /api/history/{id}
- **响应**：JSON，操作成功/失败状态

## 7. 验收标准

| 编号 | 验收项 | 标准 |
|------|--------|------|
| AC-001 | 质量预测 | 输入参数后能返回3类缺陷预测结果及概率 |
| AC-002 | 异常检测 | 参数超范围时给出对应提醒 |
| AC-003 | 数据分析 | 分析页面正常展示图表和统计数据 |
| AC-004 | 历史记录 | 预测后自动保存，可分页查询和删除 |
| AC-005 | 一键启动 | 双击run.bat可完成检查并启动服务 |
| AC-006 | 自动化测试 | 运行test_app.py全部测试通过 |
| AC-007 | 模型准确率 | 测试集平均准确率 ≥ 85% |
