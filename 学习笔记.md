# 制造智能技术课程设计｜阶段学习笔记
> 阶段任务：GitHub搭建、Vibe Coding学习、Git原理、AI工具选型、选题调研
## 1. GitHub账号与仓库说明
1. GitHub账号：填写自己github用户名
2. 仓库地址：填写你的github仓库链接
3. 仓库用途：用于本课程设计全部版本管理，保存学习笔记、需求文档、项目源代码、prompt日志、readme等，所有改动使用git小步提交，保留完整提交历史。

## 2. Git原理与功能学习
### Git核心原理
Git是分布式版本控制系统，每个本地副本都拥有完整的版本仓库，不依赖单一服务器。工作分为工作区、暂存区、本地仓库、远程仓库四个区域。
- **工作区**：本地实际编辑的文件
- **暂存区**：临时存放待提交变更
- **本地仓库**：本机保存完整提交历史
- **远程仓库(GitHub)**：云端托管代码仓库，多人/个人备份、版本同步。

### 常用核心功能
1. `git init`：初始化本地仓库
2. `git add`：把文件加入暂存区
3. `git commit -m "提交说明"`：提交变更到本地仓库，必须写清晰有意义提交注释
4. `git remote add origin`：绑定远程github仓库
5. `git push`：推送本地版本到远程GitHub仓库
6. `git pull`：拉取远程仓库更新到本地
7. `git log`：查看全部提交历史，本课程设计要求小步提交，保留完整过程档案。

> 课程设计要求：每次功能改动做小步提交，不能一次性大段提交，提交说明清晰，用于过程考核。

## 3. Vibe Coding与AI Harness工具、模型选型
### 3.1 Vibe Coding方法理解
Vibe Coding是AI辅助开发的工作模式，核心要点：
1. **提示词工程**：任务拆解、写清楚约束条件、明确验收标准，把大项目拆分成小任务交给AI。
2. **上下文工程**：维护项目规则文档，让AI读取已有代码再迭代修改，减少幻觉错误。
3. **规格驱动开发**：先写需求文档→AI生成开发计划→分步实现→分步验收。
4. **自动化测试+人工审查**：AI生成代码不能直接使用，人工阅读审查逻辑，编写简单自动化测试验证功能正确性。
5. **调试接管策略**：AI代码出错时，回滚版本、缩小改动范围、最小复现问题，人工介入调试，不直接全盘相信AI输出。

### 3.2 Harness（AI编程工具）拟选用
> 任务书参考工具列表中选择：**通义灵码 + Claude Code**
1. 通义灵码：IDE插件，本地写代码时实时补全、调试、代码解释，适合后端、数据库、前端快速开发。
2. Claude Code：适合生成完整模块代码、生成需求文档、提示词编写、大段业务逻辑实现。

### 3.3 底层大模型拟选用
选择 **Qwen3.8**，开源工业代码能力强，支持长上下文，适合处理完整项目代码文件。

### 3.4 使用约束（课程设计要求）
1. 所有AI生成代码必须人工阅读、理解，不直接复制粘贴不理解的代码。
2. 完整保存prompt会话日志，作为过程档案提交。
3. 报告中需要披露工具、prompt策略、AI出错案例以及自己如何修正。

## 4. 选题调研
### 选题题目
基于机器学习的工业设备轴承故障智能检测与运维预警系统

### 业务背景
旋转机械轴承是工业设备核心零部件，轴承故障是设备停机的重要诱因。传统依靠人工巡检效率低，故障发现滞后。使用振动信号+人工智能，实现故障自动识别、剩余寿命预测，完成设备智能运维，属于典型工业设备运维场景，符合制造智能课程设计场景要求。

### 数据集来源
凯斯西储大学CWRU轴承故障数据集，公开网上可下载，工业故障检测领域标准公开数据集。
数据集包含：不同负载、不同故障类型（正常、内圈故障、外圈故障、滚动体故障）的振动采集时序信号。

### 计划使用课程技术（至少3项）
1. **信号处理与特征工程**：对原始振动信号做滤波降噪，提取时域、频域特征，为模型准备输入数据。
2. **机器学习故障分类**：使用SVM/随机森林完成轴承多类别故障识别，判断当前设备故障类型。
3. **LSTM时序预测**：基于时序振动数据实现设备剩余使用寿命(RUL)预测，输出寿命预警。

> 可选扩展：增加专家规则推理，输出维修建议，第4个技术点。

### 系统架构简述（B/S）
1. **前端UI**：浏览器网页；实现数据上传、故障检测页面、预测结果展示、告警记录查询。
2. **后端服务**：FastAPI实现API接口，承接前端请求，调用算法模块。
3. **数据库MySQL**：存储设备台账、上传振动原始数据、特征数据、故障检测记录、预警告警记录。
4. **算法模块**：信号特征提取模块、故障分类模块、LSTM寿命预测模块。

### 业务流程闭环
1. 用户上传轴承振动数据集文件
2. 后端执行信号预处理、特征提取
3. 调用分类算法识别当前轴承故障类型
4. LSTM模型预测剩余使用寿命
5. 数据库保存检测结果
6. 前端展示故障结果、寿命预警，给出运维维修建议。

## 5.本阶段后续计划
1. 完成本地git与github远程仓库绑定，把本笔记推送远程仓库。
2. 调研CWRU数据集，了解数据格式。
3. 学习vibe coding开发流程，整理后续项目的提示词模板。
4. 后续阶段：需求规格说明书编写，数据集清洗，后端、数据库、前端、算法模块开发。
